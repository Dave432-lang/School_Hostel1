# Environment Variables — School Hostel Booking System
# Server
PORT=5000

# Database (MySQL via XAMPP)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=school_hostel
DB_USER=root
DB_PASSWORD=

# JWT
JWT_SECRET=hostel_super_secret_key_change_this_in_production
JWT_EXPIRES=7d

# Paystack (Optional for local testing)
PAYSTACK_SECRET=sk_test_placeholder_key
